import Foundation
import Firebase
import FirebaseStorage
import SwiftUI;

class CustomerEditProfile: ObservableObject {
    
    func updateProfileImage() {
        let storageRef = Storage.storage()
//        let imageRef = storageRef.chil
    }
}
